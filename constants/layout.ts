/** Gap between the floating bottom panel and the tab bar / safe-area edge */
export const FLOATING_PANEL_GAP = 12;

/** Extra bottom padding so scroll content is not hidden behind a single floating button */
export const CONTENT_BOTTOM_PADDING = 96;

/** Extra bottom padding so scroll content is not hidden behind quiz's score + button panel */
export const QUIZ_CONTENT_BOTTOM_PADDING = 156;
